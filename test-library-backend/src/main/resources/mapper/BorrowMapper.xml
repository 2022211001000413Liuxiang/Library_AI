<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.BorrowMapper">

    <resultMap id="BaseResultMap" type="com.library.entity.Borrow">
        <id column="id" property="id"/>
        <result column="book_id" property="bookId"/>
        <result column="user_id" property="userId"/>
        <result column="borrow_date" property="borrowDate"/>
        <result column="due_date" property="dueDate"/>
        <result column="return_date" property="returnDate"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="bookName" property="bookName"/>
        <result column="userName" property="userName"/>
    </resultMap>
<!--单页数据查询-->
    <select id="selectBorrowPage" resultMap="BaseResultMap">
        SELECT b.id, b.book_id, b.user_id, b.borrow_date, b.due_date, b.return_date,
               CASE WHEN b.status = 0 AND b.due_date &lt; CURDATE() THEN 2 ELSE b.status END AS status,
               b.create_time, b.update_time,
               bk.name as bookName, u.name as userName, u.avatar as userAvatar
        FROM borrow b
        LEFT JOIN book bk ON b.book_id = bk.id
        LEFT JOIN sys_user u ON b.user_id = u.id
        <where>
            <if test="bookName != null and bookName != ''">
                AND bk.name LIKE CONCAT('%', #{bookName}, '%')
            </if>
            <if test="userName != null and userName != ''">
                AND u.name LIKE CONCAT('%', #{userName}, '%')
            </if>
            <if test="status != null and status == 0">
                AND b.status = 0 AND b.due_date >= CURDATE()
            </if>
            <if test="status != null and status == 1">
                AND b.status = 1
            </if>
            <if test="status != null and status == 2">
                AND (b.status = 2 OR (b.status = 0 AND b.due_date &lt; CURDATE()))
            </if>
        </where>
        ORDER BY b.create_time DESC
    </select>
<!--记录总数-->
    <select id="selectBorrowPageCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM borrow b
        LEFT JOIN book bk ON b.book_id = bk.id
        LEFT JOIN sys_user u ON b.user_id = u.id
        <where>
            <if test="bookName != null and bookName != ''">
                AND bk.name LIKE CONCAT('%', #{bookName}, '%')
            </if>
            <if test="userName != null and userName != ''">
                AND u.name LIKE CONCAT('%', #{userName}, '%')
            </if>
            <if test="status != null and status == 0">
                AND b.status = 0 AND b.due_date >= CURDATE()
            </if>
            <if test="status != null and status == 1">
                AND b.status = 1
            </if>
            <if test="status != null and status == 2">
                AND (b.status = 2 OR (b.status = 0 AND b.due_date &lt; CURDATE()))
            </if>
        </where>
    </select>
<!--根据id查借阅记录-->
    <select id="selectBorrowPageByUserId" resultMap="BaseResultMap">
        SELECT b.id, b.book_id, b.user_id, b.borrow_date, b.due_date, b.return_date,
               CASE WHEN b.status = 0 AND b.due_date &lt; CURDATE() THEN 2 ELSE b.status END AS status,
               b.create_time, b.update_time,
               bk.name as bookName, u.name as userName
        FROM borrow b
        LEFT JOIN book bk ON b.book_id = bk.id
        LEFT JOIN sys_user u ON b.user_id = u.id
        <where>
            b.user_id = #{userId}
            <if test="status != null and status == 0">
                AND b.status = 0 AND b.due_date >= CURDATE()
            </if>
            <if test="status != null and status == 1">
                AND b.status = 1
            </if>
            <if test="status != null and status == 2">
                AND (b.status = 2 OR (b.status = 0 AND b.due_date &lt; CURDATE()))
            </if>
        </where>
        ORDER BY b.create_time DESC
    </select>
<!--查询单条详情-->
    <select id="selectBorrowById" resultMap="BaseResultMap">
        SELECT b.id, b.book_id, b.user_id, b.borrow_date, b.due_date, b.return_date,
               CASE WHEN b.status = 0 AND b.due_date &lt; CURDATE() THEN 2 ELSE b.status END AS status,
               b.create_time, b.update_time,
               bk.name as bookName, u.name as userName
        FROM borrow b
        LEFT JOIN book bk ON b.book_id = bk.id
        LEFT JOIN sys_user u ON b.user_id = u.id
        WHERE b.id = #{id}
    </select>

    <!-- 按月借阅趋势 -->
    <select id="selectMonthlyTrend" resultType="java.util.Map">
        SELECT DATE_FORMAT(borrow_date, '%Y-%m') as month, COUNT(*) as count
        FROM borrow
        WHERE borrow_date >= DATE_SUB(CURDATE(), INTERVAL #{months} MONTH)
        GROUP BY DATE_FORMAT(borrow_date, '%Y-%m')
        ORDER BY month ASC
    </select>

    <!-- 热门图书 Top N -->
    <select id="selectTopBooks" resultType="java.util.Map">
        SELECT bk.name as name, COUNT(*) as count
        FROM borrow b
        LEFT JOIN book bk ON b.book_id = bk.id
        GROUP BY b.book_id, bk.name
        ORDER BY count DESC
        LIMIT #{limit}
    </select>

</mapper>
